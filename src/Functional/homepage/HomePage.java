package Functional.homepage;

import org.openqa.selenium.WebElement;



/**
 * Functional module for HomePage actions. Provides steps
 * for actions related to HomePage, e.g.
 * <ul>
 * <li>sampleMethod() - basic step
 * <li>sampleMethod2() - another step
 * </ul>
 * 
 */
public class HomePage {

	//TODO - page declarations (e.g. HomePagePage homepagePage = null; 
	
	/**
	 * Creates new instance of the HomePage module.<br><br>
	 * Holds initializers for the test parameters and page
	 * classes that are used within.
	 */
	public HomePage(){
	
		//TODO - page initialization (e.g. homepagePage = new HomePagePage(ptp.getDriver());
	}
	
	
}
